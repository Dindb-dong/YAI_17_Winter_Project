# Text-to-Video Temporal Retrieval: 프로젝트 발전 기록

이 문서는 본 프로젝트가 어떤 문제를 풀기 위해 어떻게 발전해왔는지, 단계별 의사결정과 로직 변화를 정리한 기록입니다.

## 프로젝트 한 줄 요약
사용자의 자연어 텍스트를 입력받아 영상에서 관련 장면을 찾고, 이후 단일 장면 검색을 넘어 연속 구간(시퀀스)까지 안정적으로 추정하는 검색 엔진으로 발전.

## 1차 단계: 장면 검색 (Window Retrieval)

### 목표
자연어 쿼리에 해당하는 장면을 영상에서 빠르게 찾기.

### 기본 Workflow
1. Input: 사용자 쿼리(Text) + 영상(Video File)
2. Preprocessing: 영상을 `p`초 단위 sliding window로 분할하고 window마다 `q`개 프레임 균등 샘플링
3. Matching:
   - CLIP으로 텍스트-프레임 코사인 유사도 계산
   - BLIP-2로 캡션 생성 후 쿼리와 의미 비교(선택 실험)
4. Score Aggregation: window 내 프레임 점수 집계로 window 점수 산출
5. Ranking: 상위 K개 window를 최종 후보로 제시

### 1차 이후 핵심 업데이트
1. BLIP-2 기본 비활성화
- 쿼리를 힌트로 준 캡션 생성은 편향 가능성이 있어 기본 경로에서 제외.
- 현재 코드는 옵션으로 남아 있으나(`USE_BLIP`), 기본 운영 경로는 CLIP 중심.

2. `step` 도입으로 window 중첩
- 빠른 장면 전환을 놓치지 않기 위해 고정 window가 아닌 겹치는 스캔 방식으로 변경.

3. window 대표 점수 변경
- 평균 중심 집계에서, 쿼리와 가장 강하게 매칭된 프레임 중심(`max`)으로 전환.

4. 검색 유형 분리
- 단일 동작 검색과 연속 동작 검색 로직을 분리해 처리.

5. Gemini 기반 쿼리 분할/영문화
- 한국어 원문을 직접 CLIP에 넣는 것보다 영어 기술 문장이 유리하므로,
- Gemini를 이용해 최대 2개 동작으로 분할 + 영어 변환.

## 2차 단계: 구간 검색 (Temporal Sequence Retrieval)

### 목표
단일 스냅샷이 아닌, 시간적으로 이어진 의미 구간(sequence)을 찾기.

### 확장 Workflow
1. Input: 사용자 쿼리 + 영상
2. Sliding window + 샘플링
3. CLIP 기반 유사도 계산
4. Sequence Localization (Top-K anchor 기반)
- Top-K Selection: 상위 후보를 anchor로 선택
- Boundary Expansion: anchor 기준 좌/우로 확장하며 경계 탐색
- Bidirectional Search: 좌/우 탐색 결과를 결합해 구간 생성
- Score Aggregation: 전역 유사도 흐름을 활용해 신뢰도 산정

### 2차 이후 핵심 업데이트
1. 단일 하락점이 아닌 다중 하락 후보 비교
- 유사도 하락이 1회 발생했다고 바로 끊지 않고,
- 2~3회 하락 후보까지 추적 후 가장 타당한 경계를 선택.

2. 2개 쿼리 분할 시 구간 결합
- 쿼리가 2개로 분할되면 각각 EMA 경계를 계산하고 최종 구간은 합집합으로 결정.

3. 긴 영상 속도 개선
- 문제: 영상 길이가 길어질수록 CLIP 연산 시간이 급증.
- 해결방안 1: Filtering Mode
  - 초기 추세를 만들다가 Top-K가 안정되면 필터링 모드 ON.
  - 이후 1차로 `q/3`만 빠르게 샘플링.
  - 점수가 `Top-K 최하위 점수의 70%` 미만이면 해당 window 스킵.
  - 정답 구간이 앞쪽에 있을수록 속도 이득이 커짐.
- 해결방안 2: `q` 기본값 하향
  - `q=12`에서 `q=4~6`까지 실험, 장면 변화 밀도를 고려할 때 정확도/속도 균형 개선.

4. 결론적 설계
- Top-K 후보 중심으로 전역 프레임 점수를 재활용하고,
- anchor 주변 전후 점수 흐름에 EMA를 적용해 구간을 정밀화.

## 3차 단계: 경계 안정화 (Robust Boundarying)

### 목표
경계 확장이 과도하게 짧거나 길어지는 불안정성 제거.

### 발견한 문제
반복문 기반 좌/우 확장만으로 경계를 정하면,
- 너무 이르게 멈추거나
- 너무 길게 확장되는 케이스가 발생.

### 3차 이후 업데이트
1. top window의 `max score` 기준으로 후보 프레임을 선별
2. 해당 기준(예: 상위 75% 이상 점수)을 만족하는 프레임들 중
   - 가장 빠른 시점과 가장 늦은 시점을 초기 경계로 설정
3. 기준을 점진적으로 높이며 구간을 좁혀 최종 경계 확정

## 현재 로직 관점 정리

1. 검색 정확도
- 단일 동작/연속 동작 분리 처리 + 쿼리 분할 + EMA 정밀화로 개선.

2. 검색 속도
- 2단계 샘플링(`q/3 -> q`) + 필터링 스킵으로 긴 영상 처리 성능 개선.

3. 실무적 안정성
- API 실패 시 rule-based fallback,
- 전역 유사도 재활용,
- 경계 탐색 안정화 로직 추가.

## 이후 개선 후보

1. 경계 결정 기준 자동 튜닝(영상 장르/길이별 threshold 적응)
2. 쿼리 분할 품질 평가 메트릭 정립(분할 정확도 vs retrieval 성능)
3. BLIP 대체 경량 semantic reranker 검토(편향 완화 + 비용 절감)
